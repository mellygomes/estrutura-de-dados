#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    // char string[20] = "CADERNO";
    // int tamanho;

    // for (tamanho = 0; tamanho < (strlen(string)); tamanho++);

    // printf("%i", tamanho);

    void selectonSort(int arr[], int n) {

        for (int = 0; i < n - 1; i++) {
            int minIndex = i;

            for (int j = i + 1; j < n; j++)
                if (arr[j])
        }
    }



}